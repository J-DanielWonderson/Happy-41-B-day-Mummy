<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1,user-scalable=no">
<title>Happy 11th Birthday, Joseph! 🚀</title>
<link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Rajdhani:wght@300;400;600;700&family=Playfair+Display:ital@1&display=swap" rel="stylesheet">
<style>
/* ══════════════════════════════════════════════
   DESIGN TOKENS
══════════════════════════════════════════════ */
:root{
  --gold:#f5c518; --gold2:#ffd700; --hot:#ff1a6c; --cyan:#00e8ff;
  --purple:#8b2be2; --green:#00ff88; --orange:#ff5a00;
  --deep:#020510; --panel:rgba(0,20,50,0.72);
  --border:rgba(0,232,255,0.22); --border-hot:rgba(255,26,108,0.35);
  --glow-c:0 0 20px rgba(0,232,255,0.4);
  --ease:cubic-bezier(0.34,1.56,0.64,1);
  --fast:cubic-bezier(0.25,0.46,0.45,0.94);
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html,body{width:100%;height:100%;overflow:hidden;font-family:'Rajdhani',sans-serif;
  color:#e8f4ff;background:var(--deep);cursor:none;
  -webkit-font-smoothing:antialiased}

/* ── SCANLINES OVERLAY ── */
body::after{content:'';position:fixed;inset:0;z-index:9990;pointer-events:none;
  background:repeating-linear-gradient(0deg,transparent,transparent 2px,rgba(0,0,0,0.04) 2px,rgba(0,0,0,0.04) 4px);
  animation:scanDrift 8s linear infinite}
@keyframes scanDrift{from{background-position:0 0}to{background-position:0 40px}}

/* ── CURSOR ── */
#cur{position:fixed;width:10px;height:10px;border-radius:50%;background:var(--cyan);
  pointer-events:none;z-index:9999;transform:translate(-50%,-50%);mix-blend-mode:screen;
  box-shadow:0 0 12px var(--cyan),0 0 24px rgba(0,232,255,0.5);transition:transform .04s,width .15s,height .15s}
#cur-ring{position:fixed;width:32px;height:32px;border:1px solid rgba(0,232,255,0.5);border-radius:50%;
  pointer-events:none;z-index:9998;transform:translate(-50%,-50%)}
.c-spark{position:fixed;pointer-events:none;z-index:9997;width:3px;height:3px;border-radius:50%;
  transform:translate(-50%,-50%);animation:cSpark .55s ease-out forwards}
@keyframes cSpark{100%{transform:translate(calc(-50% + var(--dx)),calc(-50% + var(--dy))) scale(0);opacity:0}}

/* ── GLOBAL CANVASES ── */
#bgc,#bloom-overlay,#burst-canvas{position:fixed;inset:0;pointer-events:none}
#bgc{z-index:0}
#bloom-overlay{z-index:2;mix-blend-mode:screen;filter:blur(12px);opacity:0.55}
#burst-canvas{z-index:500}

/* ── SCENES ── */
.scene{position:fixed;inset:0;z-index:10;display:flex;flex-direction:column;
  align-items:center;justify-content:center;opacity:0;pointer-events:none;will-change:opacity,transform}
.scene.enter{animation:sIn .8s var(--ease) forwards;pointer-events:all}
.scene.exit{animation:sOut .48s var(--fast) forwards;pointer-events:none}
@keyframes sIn{from{opacity:0;transform:scale(.93) translateY(28px);filter:blur(14px)}
               to{opacity:1;transform:scale(1) translateY(0);filter:blur(0)}}
@keyframes sOut{from{opacity:1;transform:scale(1);filter:blur(0)}
                to{opacity:0;transform:scale(1.07) translateY(-22px);filter:blur(10px)}}

/* ── HUD CORNER BRACKETS (reusable) ── */
.hud-box{position:relative}
.hud-box::before,.hud-box::after{content:'';position:absolute;width:14px;height:14px;border-color:var(--cyan);border-style:solid;pointer-events:none}
.hud-box::before{top:-2px;left:-2px;border-width:2px 0 0 2px}
.hud-box::after{bottom:-2px;right:-2px;border-width:0 2px 2px 0}

/* ── MISSION COUNTER (top right) ── */
#mission-ctr{position:fixed;top:18px;right:clamp(16px,3vw,28px);z-index:200;
  font-family:'Orbitron',monospace;font-size:clamp(0.6rem,1.2vw,0.72rem);
  letter-spacing:0.2em;color:rgba(0,232,255,0.55);pointer-events:none}
#mission-ctr span{color:var(--cyan)}

/* ── PROGRESS BAR ── */
.pdots{position:fixed;bottom:18px;left:50%;transform:translateX(-50%);
  display:flex;gap:8px;z-index:200;pointer-events:none;align-items:center}
.pd{width:5px;height:5px;border-radius:50%;background:rgba(255,255,255,.15);transition:all .35s}
.pd.active{background:var(--cyan);box-shadow:0 0 8px var(--cyan),0 0 16px rgba(0,232,255,0.4);width:22px;border-radius:2px}
.pd.done{background:rgba(0,232,255,0.38)}
.pd-label{font-family:'Orbitron',monospace;font-size:0.55rem;letter-spacing:0.1em;
  color:rgba(0,232,255,0.4);margin-left:6px}

/* ── CONFETTI ── */
.cfetti{position:fixed;top:-14px;pointer-events:none;z-index:1000;border-radius:2px;
  animation:cfFall var(--d) var(--dl) ease-in both}
@keyframes cfFall{0%{opacity:1;transform:translateY(0) rotate(0) translateX(0)}
                  100%{opacity:.15;transform:translateY(110vh) rotate(var(--r)) translateX(var(--x))}}

/* ── SHARED ANIMS ── */
@keyframes fadeUp{from{opacity:0;transform:translateY(18px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
@keyframes ripOut{to{width:160px;height:160px;opacity:0;border-color:var(--cyan)}}
@keyframes bigIn{from{opacity:0;transform:scale(.35)}60%{opacity:1;transform:scale(1.08)}to{opacity:1;transform:scale(1)}}
@keyframes glitchFlash{
  0%,100%{clip-path:none;transform:none}
  10%{clip-path:polygon(0 20%,100% 20%,100% 40%,0 40%);transform:translate(-3px,0)}
  20%{clip-path:none;transform:none}
  30%{clip-path:polygon(0 60%,100% 60%,100% 75%,0 75%);transform:translate(3px,0)}
  40%{clip-path:none;transform:none}
}

/* ══════════ SCENE 0 — INTRO ══════════ */
#s0{background:radial-gradient(ellipse 90% 75% at 50% 56%,#0d0535,var(--deep) 80%)}
/* Grid texture */
#s0::before{content:'';position:absolute;inset:0;
  background-image:linear-gradient(rgba(0,232,255,0.035) 1px,transparent 1px),
    linear-gradient(90deg,rgba(0,232,255,0.035) 1px,transparent 1px);
  background-size:60px 60px;pointer-events:none}
.n11{font-family:'Orbitron',monospace;font-weight:900;font-size:clamp(80px,22vw,210px);line-height:1;
  background:linear-gradient(145deg,rgba(255,255,255,0.3) 0%,var(--gold) 30%,#fff 52%,var(--gold2) 68%,rgba(255,255,255,0.2) 100%);
  -webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent;
  filter:drop-shadow(0 0 40px rgba(245,197,24,.8)) drop-shadow(0 0 80px rgba(245,197,24,.3));
  opacity:0;animation:numCrash 1s .35s var(--ease) forwards;position:relative;z-index:2}
@keyframes numCrash{from{opacity:0;transform:scale(.15) rotate(-10deg);filter:blur(24px)}
  55%{opacity:1;transform:scale(1.14) rotate(1.5deg)}
  to{opacity:1;transform:scale(1) rotate(0);filter:drop-shadow(0 0 40px rgba(245,197,24,.8))}}
.n11.glitch{animation:numCrash 1s .35s var(--ease) forwards,glitchFlash 0.4s 3s ease-out}
.n-name{font-family:'Orbitron',monospace;font-size:clamp(1rem,3vw,1.7rem);font-weight:700;
  letter-spacing:0.25em;text-transform:uppercase;
  color:var(--cyan);text-shadow:0 0 20px var(--cyan),0 0 40px rgba(0,232,255,0.4);
  opacity:0;animation:fadeUp .7s 1.1s ease forwards;margin-bottom:4px}
.n-sub{font-family:'Orbitron',monospace;font-size:clamp(.6rem,1.5vw,.85rem);
  letter-spacing:.35em;text-transform:uppercase;color:rgba(255,255,255,.45);
  opacity:0;animation:fadeUp .6s 1.5s ease forwards;margin-bottom:36px}
.tap-h{font-family:'Rajdhani',sans-serif;font-size:clamp(.65rem,1.4vw,.78rem);
  letter-spacing:.3em;text-transform:uppercase;color:rgba(0,232,255,0.4);
  opacity:0;animation:fadeIn .5s 2.2s ease forwards,tapB 2s 2.7s ease-in-out infinite;
  position:absolute;bottom:60px;display:flex;align-items:center;gap:10px}
.tap-h::before,.tap-h::after{content:'';width:30px;height:1px;background:currentColor;display:block}
@keyframes tapB{0%,100%{opacity:.25;letter-spacing:.3em}50%{opacity:.75;letter-spacing:.4em}}
.i-ring{position:absolute;border-radius:50%;border:1px solid rgba(0,232,255,0.08);
  opacity:0;pointer-events:none;animation:iRX var(--t) var(--dl) ease-out infinite}
@keyframes iRX{0%{transform:translate(-50%,-50%) scale(.05);opacity:.5}100%{transform:translate(-50%,-50%) scale(1);opacity:0}}

/* ══════════ SCENE 1 — CAKE ══════════ */
#s1{background:radial-gradient(ellipse 130% 85% at 50% 110%,#3d0e00,#1e002a 40%,var(--deep) 72%);overflow:hidden}
.sspot{position:absolute;bottom:-60px;left:50%;transform:translateX(-50%);
  width:520px;height:220px;background:radial-gradient(ellipse,rgba(255,140,30,.18),transparent 70%);
  pointer-events:none;z-index:1;animation:spB 3.5s ease-in-out infinite alternate}
@keyframes spB{from{opacity:.5}to{opacity:.95}}
.sfloor{position:absolute;bottom:0;left:0;right:0;height:30%;background:linear-gradient(0deg,rgba(0,0,0,.55),transparent);pointer-events:none;z-index:1}
.c-stage{position:relative;z-index:2;display:flex;flex-direction:column;align-items:center}
.wish{font-family:'Playfair Display',serif;font-style:italic;font-size:clamp(.95rem,3.2vw,1.8rem);
  color:rgba(255,220,140,.95);letter-spacing:.06em;
  text-shadow:0 0 30px rgba(255,180,50,.55),0 2px 4px rgba(0,0,0,.5);
  opacity:0;animation:fadeUp .7s .3s ease forwards;margin-bottom:22px}

/* CANDLES */
.c-row{display:flex;gap:clamp(18px,3.5vw,32px);align-items:flex-end;position:relative;z-index:20;margin-bottom:-2px}
.candle{display:flex;flex-direction:column;align-items:center;cursor:pointer;position:relative;-webkit-tap-highlight-color:transparent}
.fw{position:relative;width:24px;height:40px;display:flex;align-items:flex-end;justify-content:center;margin-bottom:-3px}
.fglow{position:absolute;bottom:-8px;left:50%;transform:translateX(-50%);width:48px;height:48px;border-radius:50%;
  background:radial-gradient(rgba(255,170,0,.4),transparent 70%);animation:fG .22s ease-in-out infinite alternate}
@keyframes fG{from{opacity:.45;transform:translateX(-50%) scale(.88)}to{opacity:1;transform:translateX(-50%) scale(1.12)}}
.fo{position:absolute;bottom:0;width:18px;height:32px;border-radius:50% 50% 28% 28%/60% 60% 40% 40%;
  background:radial-gradient(ellipse at 50% 88%,#ff6a00,#ee0979 55%,transparent 82%);
  animation:flO .17s ease-in-out infinite alternate;filter:blur(1.5px)}
@keyframes flO{from{transform:scaleX(1) skewX(0)}to{transform:scaleX(.84) skewX(7deg) scaleY(1.05)}}
.fm{position:absolute;bottom:0;width:12px;height:26px;border-radius:50% 50% 28% 28%/60% 60% 40% 40%;
  background:radial-gradient(ellipse at 50% 78%,#ffe000,#ff9500 60%,transparent);
  animation:flM .14s .05s ease-in-out infinite alternate;filter:blur(.5px)}
@keyframes flM{from{transform:scaleX(1)}to{transform:scaleX(.86) skewX(-6deg)}}
.fi{position:absolute;bottom:2px;width:6px;height:18px;border-radius:50% 50% 30% 30%/60% 60% 40% 40%;
  background:linear-gradient(180deg,#fff,#fffde7 55%,transparent);animation:flI .1s ease-in-out infinite alternate}
@keyframes flI{from{transform:scaleX(1)}to{transform:scaleX(.78) translateY(1px)}}
.fw.out{display:none}
.c-smk{position:absolute;top:-52px;left:50%;transform:translateX(-50%);width:4px;pointer-events:none}
.smkp{position:absolute;width:7px;height:7px;border-radius:50%;background:rgba(200,200,200,.5);bottom:0}
.smkp.go{animation:smkAnim var(--d) var(--dl) forwards ease-out}
@keyframes smkAnim{from{opacity:.65;transform:translateX(0) scale(1)}to{opacity:0;transform:translateX(var(--tx)) translateY(-72px) scale(3.8)}}

/* CANDLE BODY with wax melt */
.cb{width:13px;border-radius:5px 5px 3px 3px;position:relative;overflow:visible;transition:height .8s linear}
.cb::before{content:'';position:absolute;inset:0;border-radius:inherit;background:linear-gradient(90deg,rgba(255,255,255,.28) 8%,transparent 55%,rgba(0,0,0,.18) 95%)}
.cwick{position:absolute;top:-5px;left:50%;transform:translateX(-50%);width:2px;height:8px;background:#2a1600;border-radius:1px;z-index:5}
.cdrip{position:absolute;border-radius:0 0 50% 50%;z-index:3;transition:height .5s}
.candle:nth-child(1) .cb{background:linear-gradient(180deg,#ff9ab0,#ff4d6d,#c9184a)}
.candle:nth-child(1) .cdrip{background:#ff9ab0}
.candle:nth-child(2) .cb{background:linear-gradient(180deg,#b4e0ff,#4cc9f0,#4361ee)}
.candle:nth-child(2) .cdrip{background:#b4e0ff}
.candle:nth-child(3) .cb{background:linear-gradient(180deg,#b7e4c7,#52b788,#1b4332)}
.candle:nth-child(3) .cdrip{background:#b7e4c7}

/* CAKE LAYERS */
.ck-w{position:relative;display:flex;flex-direction:column;align-items:center;
  filter:drop-shadow(0 28px 80px rgba(0,0,0,.92));cursor:pointer;transition:transform .3s var(--ease)}
.ck-w:hover{transform:translateY(-6px)}
.ckL{position:relative;border-radius:16px;overflow:visible}
.ckT{width:clamp(138px,26vw,186px);height:clamp(48px,8vh,66px);z-index:3}
.ckM{width:clamp(178px,34vw,246px);height:clamp(54px,9vh,76px);z-index:2;margin-top:-5px}
.ckB{width:clamp(216px,42vw,296px);height:clamp(60px,10vh,86px);z-index:1;margin-top:-5px}
.ck-fill{position:absolute;inset:0;border-radius:inherit;overflow:hidden}
.ckT .ck-fill{background:linear-gradient(180deg,#d03c78,#a01e52 45%,#821440 100%)}
.ckM .ck-fill{background:linear-gradient(180deg,#dc6c0c,#b04400 45%,#8e3400 100%)}
.ckB .ck-fill{background:linear-gradient(180deg,#b41c54,#8e0a38 45%,#6e062c 100%)}
.ck-fill::after{content:'';position:absolute;inset:0;background:repeating-linear-gradient(90deg,transparent,transparent 18px,rgba(255,255,255,.022) 18px,rgba(255,255,255,.022) 19px)}
.ck-fr{position:absolute;top:-14px;left:-1px;right:-1px;height:26px;border-radius:32px 32px 0 0;z-index:5}
.ckT .ck-fr{background:linear-gradient(180deg,#fff9ee,#ffe9ca);box-shadow:0 2px 10px rgba(0,0,0,.22)}
.ckM .ck-fr{background:linear-gradient(180deg,#fff1d2,#ffd89a);box-shadow:0 2px 10px rgba(0,0,0,.22)}
.ckB .ck-fr{background:linear-gradient(180deg,#fff9ee,#ffe9ca);box-shadow:0 2px 10px rgba(0,0,0,.22)}
.ck-fr::after{content:'';position:absolute;top:28%;left:4%;right:4%;height:36%;border-radius:50%;background:rgba(255,255,255,.38);transform:scaleY(.38)}
.ck-plate{width:clamp(238px,48vw,328px);height:20px;background:linear-gradient(180deg,#e8cc80,#c6a33c,#987216);
  border-radius:50%;margin-top:-4px;z-index:0;box-shadow:0 10px 26px rgba(0,0,0,.72),inset 0 2px 4px rgba(255,255,255,.2);position:relative}
.ck-plate::after{content:'';position:absolute;top:3px;left:10%;right:10%;height:5px;border-radius:50%;background:rgba(255,255,255,.16)}
.ck-hint{font-family:'Rajdhani',sans-serif;font-size:clamp(.6rem,1.2vw,.7rem);letter-spacing:.16em;text-transform:uppercase;
  color:rgba(0,232,255,.3);opacity:0;animation:fadeUp .5s .95s ease forwards;margin-top:18px;text-align:center}

/* Cake action area */
.cake-actions{display:flex;flex-direction:column;align-items:center;gap:10px;margin-top:14px}
.blow-btn{padding:clamp(9px,1.5vh,13px) clamp(22px,4vw,42px);font-family:'Orbitron',monospace;font-size:clamp(.72rem,1.2vw,.88rem);font-weight:700;letter-spacing:.14em;text-transform:uppercase;
  border:1.5px solid rgba(245,197,24,.6);border-radius:4px;background:rgba(245,197,24,.07);color:var(--gold);cursor:pointer;
  box-shadow:0 0 18px rgba(245,197,24,.15),inset 0 0 12px rgba(245,197,24,.04);
  transition:all .25s var(--ease);opacity:0;animation:fadeUp .5s 1.25s ease forwards;position:relative;overflow:hidden;clip-path:polygon(8px 0,100% 0,100% calc(100% - 8px),calc(100% - 8px) 100%,0 100%,0 8px)}
.blow-btn:hover{background:rgba(245,197,24,.18);transform:scale(1.05) translateY(-2px);box-shadow:0 0 40px rgba(245,197,24,.5)}
.blow-btn::before{content:'';position:absolute;inset:0;background:linear-gradient(90deg,transparent,rgba(255,255,255,.1),transparent);transform:translateX(-100%);transition:transform .45s}
.blow-btn:hover::before{transform:translateX(100%)}

/* ══════════ SCENE 2 — BALLOONS ══════════ */
#s2{background:linear-gradient(195deg,#06001a,#140430 45%,#080018 100%)}
.bal-hdr{position:absolute;top:0;left:0;right:0;height:clamp(110px,18vh,140px);display:flex;flex-direction:column;align-items:center;justify-content:flex-end;padding-bottom:12px;z-index:5;pointer-events:none;
  background:linear-gradient(180deg,rgba(0,0,0,.6),transparent)}
.bal-title{font-family:'Orbitron',monospace;font-size:clamp(1rem,4vw,2rem);font-weight:900;
  color:var(--hot);text-shadow:0 0 24px var(--hot),0 0 50px rgba(255,26,108,.35);
  letter-spacing:.08em;text-transform:uppercase}
.pop-ctr{margin-top:8px;font-family:'Orbitron',monospace;font-size:clamp(.75rem,1.4vw,.95rem);font-weight:700;
  letter-spacing:.14em;color:#fff;padding:6px 20px;
  background:rgba(255,26,108,.12);border:1px solid rgba(255,26,108,.3);border-radius:3px;
  clip-path:polygon(6px 0,100% 0,100% calc(100% - 6px),calc(100% - 6px) 100%,0 100%,0 6px)}
/* Balloon & shadow */
.balloon{position:absolute;bottom:-220px;cursor:pointer;z-index:4;display:flex;flex-direction:column;align-items:center;-webkit-tap-highlight-color:transparent}
.b-bd{border-radius:50% 50% 47% 47%/54% 54% 46% 46%;position:relative}
.b-bd::before{content:'';position:absolute;top:11%;left:16%;width:31%;height:27%;border-radius:50%;background:rgba(255,255,255,.4);transform:rotate(-34deg);pointer-events:none}
.b-bd::after{content:'';position:absolute;top:21%;right:15%;width:11%;height:19%;border-radius:50%;background:rgba(255,255,255,.18);transform:rotate(-18deg)}
.b-shadow{position:absolute;bottom:0;left:50%;transform:translateX(-50%);border-radius:50%;pointer-events:none;z-index:3}
.b-kt{width:10px;height:13px;clip-path:polygon(25% 0,75% 0,85% 100%,15% 100%)}
.b-st{width:1px;height:80px;margin-top:-2px;background:linear-gradient(transparent,rgba(255,255,255,.18))}
.nxt-btn{position:absolute;bottom:clamp(18px,3vh,28px);left:50%;transform:translateX(-50%);
  padding:clamp(9px,1.4vh,12px) clamp(22px,4vw,38px);font-family:'Orbitron',monospace;
  font-size:clamp(.72rem,1.2vw,.85rem);font-weight:700;letter-spacing:.14em;text-transform:uppercase;
  border:1.5px solid var(--cyan);border-radius:3px;background:rgba(0,232,255,.07);color:var(--cyan);
  cursor:pointer;transition:all .28s var(--ease);z-index:10;
  box-shadow:0 0 16px rgba(0,232,255,.12);opacity:0;pointer-events:none;
  clip-path:polygon(6px 0,100% 0,100% calc(100% - 6px),calc(100% - 6px) 100%,0 100%,0 6px)}
.nxt-btn.show{opacity:1;pointer-events:all;animation:fadeUp .45s var(--ease) forwards}
.nxt-btn:hover{background:rgba(0,232,255,.18);box-shadow:0 0 40px rgba(0,232,255,.45);transform:translateX(-50%) scale(1.04)}

/* ══════════ SCENE 3 — FIREWORKS ══════════ */
#s3{background:#000}
#fw-c{position:absolute;inset:0;width:100%;height:100%}
#fw-bloom{position:absolute;inset:0;width:100%;height:100%;mix-blend-mode:screen;filter:blur(10px);opacity:.5;pointer-events:none;z-index:2}
.fw-hdr{position:absolute;top:clamp(16px,3vh,26px);left:50%;transform:translateX(-50%);text-align:center;z-index:5;pointer-events:none}
.fw-ttl{font-family:'Orbitron',monospace;font-size:clamp(.9rem,3vw,1.8rem);font-weight:900;
  color:var(--gold);text-shadow:0 0 28px var(--gold),0 0 55px rgba(245,197,24,.35);letter-spacing:.12em}
.fw-hnt{font-size:clamp(.58rem,1.1vw,.72rem);letter-spacing:.18em;text-transform:uppercase;color:rgba(255,255,255,.28);margin-top:6px;font-family:'Orbitron',monospace}

/* ══════════ SCENE 4 — ROCKET ══════════ */
#s4{background:#000}
#rk-c{position:absolute;inset:0;width:100%;height:100%}
#rk-bloom{position:absolute;inset:0;width:100%;height:100%;mix-blend-mode:screen;filter:blur(14px);opacity:.45;pointer-events:none;z-index:2}
.rk-hud{position:absolute;top:clamp(14px,2.5vh,20px);left:50%;transform:translateX(-50%);
  background:rgba(0,8,24,.75);border:1px solid rgba(0,232,255,.25);
  backdrop-filter:blur(14px);border-radius:2px;
  padding:clamp(7px,1.2vh,10px) clamp(16px,3vw,28px);
  font-family:'Orbitron',monospace;font-size:clamp(.78rem,1.3vw,1rem);font-weight:700;
  letter-spacing:.12em;color:var(--gold);text-shadow:0 0 14px var(--gold);z-index:5;white-space:nowrap;
  clip-path:polygon(8px 0,100% 0,100% calc(100% - 8px),calc(100% - 8px) 100%,0 100%,0 8px)}
.mouse-guide{position:absolute;top:50%;left:50%;transform:translate(-50%,-50%);
  font-family:'Rajdhani',sans-serif;font-size:clamp(.88rem,1.6vw,1.05rem);letter-spacing:.12em;text-transform:uppercase;
  color:rgba(0,232,255,.65);text-align:center;z-index:6;pointer-events:none;line-height:2;
  opacity:0;animation:fadeIn .5s .2s ease forwards,guideFade .8s 2.5s ease forwards}
@keyframes guideFade{to{opacity:0;transform:translate(-50%,-55%)}}
.rk-ctrl{position:absolute;bottom:clamp(14px,2.5vh,22px);display:flex;gap:14px;
  left:50%;transform:translateX(-50%);z-index:10}
.rk-btn{width:clamp(48px,8vw,62px);height:clamp(48px,8vw,62px);border-radius:50%;
  border:2px solid rgba(0,232,255,.45);background:rgba(0,232,255,.07);color:var(--cyan);
  font-size:1.3rem;cursor:pointer;display:flex;align-items:center;justify-content:center;
  transition:all .14s;-webkit-tap-highlight-color:transparent;box-shadow:0 0 12px rgba(0,232,255,.1);user-select:none;font-family:'Orbitron',monospace}
.rk-btn:active{background:rgba(0,232,255,.28);transform:scale(.9)}
.win-ov{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;
  z-index:20;background:rgba(0,0,0,.58);backdrop-filter:blur(6px);opacity:0;pointer-events:none;transition:opacity .85s}
.win-ov.show{opacity:1;pointer-events:all}
.win-ttl{font-family:'Orbitron',monospace;font-size:clamp(1.5rem,5vw,3.2rem);font-weight:900;
  color:var(--gold);text-shadow:0 0 38px var(--gold),0 0 75px rgba(255,215,0,.4);
  animation:wPop .7s var(--ease) forwards;text-align:center;padding:0 20px;line-height:1.3}
@keyframes wPop{from{transform:scale(.35);opacity:0}58%{transform:scale(1.1)}to{transform:scale(1);opacity:1}}

/* ══════════ SCENE 6 — FINALE ══════════ */
#s6{background:#000;overflow:hidden}
#fn-c{position:absolute;inset:0;width:100%;height:100%}
.fn-cnt{position:relative;z-index:5;display:flex;flex-direction:column;align-items:center;text-align:center;gap:11px;padding:20px}
.fn-badge{font-family:'Orbitron',monospace;font-size:clamp(.58rem,1.1vw,.68rem);letter-spacing:.35em;text-transform:uppercase;color:rgba(245,197,24,.55);animation:fadeUp .5s .3s ease both}
.fn-name{font-family:'Orbitron',monospace;font-size:clamp(1rem,2.8vw,1.6rem);font-weight:900;
  letter-spacing:.15em;text-transform:uppercase;color:var(--cyan);
  text-shadow:0 0 20px var(--cyan),0 0 40px rgba(0,232,255,.4);
  animation:fadeUp .6s .45s ease both}
.fn-ttl{font-family:'Orbitron',monospace;font-weight:900;font-size:clamp(1.2rem,4.5vw,3rem);animation:fadeUp .7s .6s ease both}
.fn-ttl span{display:block;background:linear-gradient(135deg,var(--gold),#ff69b4 38%,var(--cyan) 65%,var(--gold));
  background-size:300% 300%;animation:gFlow 4s ease infinite;-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent}
@keyframes gFlow{0%,100%{background-position:0% 50%}50%{background-position:100% 50%}}
.fn-num{font-family:'Orbitron',monospace;font-size:clamp(3.5rem,13vw,8.5rem);font-weight:900;line-height:1;
  background:linear-gradient(145deg,var(--gold),#fff,var(--gold2));-webkit-background-clip:text;background-clip:text;-webkit-text-fill-color:transparent;
  filter:drop-shadow(0 0 40px rgba(245,197,24,.9));animation:fadeUp .9s .85s var(--ease) both,nFloat 4s 2.5s ease-in-out infinite}
@keyframes nFloat{0%,100%{transform:translateY(0)}50%{transform:translateY(-13px)}}
.fn-sub{font-family:'Orbitron',monospace;font-size:clamp(.62rem,1.2vw,.78rem);letter-spacing:.25em;text-transform:uppercase;color:rgba(255,255,255,.38);animation:fadeUp .5s 1.1s ease both}
.restart-btn{margin-top:14px;padding:clamp(9px,1.4vh,12px) clamp(20px,3.5vw,34px);font-family:'Orbitron',monospace;font-size:clamp(.65rem,1.2vw,.8rem);font-weight:700;letter-spacing:.14em;text-transform:uppercase;
  border:1.5px solid rgba(245,197,24,.48);border-radius:3px;background:rgba(245,197,24,.07);color:var(--gold);cursor:pointer;transition:all .28s var(--ease);animation:fadeUp .5s 1.4s ease both;box-shadow:0 0 16px rgba(245,197,24,.1);clip-path:polygon(6px 0,100% 0,100% calc(100% - 6px),calc(100% - 6px) 100%,0 100%,0 6px)}
.restart-btn:hover{background:rgba(245,197,24,.18);transform:scale(1.05);box-shadow:0 0 36px rgba(245,197,24,.45)}

/* ══════════════════════════════════════════════════
   SCENE 5 — ENVELOPE  (exact port of reference)
══════════════════════════════════════════════════ */
#s5{
  background:radial-gradient(ellipse 80% 68% at 50% 52%,#080d20,var(--deep) 78%);
}
#s5::before{
  content:'';position:absolute;inset:0;pointer-events:none;
  background-image:
    linear-gradient(rgba(0,232,255,.022) 1px,transparent 1px),
    linear-gradient(90deg,rgba(0,232,255,.022) 1px,transparent 1px);
  background-size:54px 54px;
}
.env-wrapper{
  display:flex;flex-direction:column;align-items:center;gap:28px;
  position:relative;z-index:5;
}
.env-title{
  font-family:'Orbitron',monospace;font-size:clamp(.68rem,1.4vw,.88rem);
  font-weight:700;letter-spacing:.3em;text-transform:uppercase;
  color:rgba(0,232,255,.5);
}
.env-scale{ transform-origin:top center; }
@media(max-width:360px){.env-scale{transform:scale(.72);}}
@media(min-width:361px) and (max-width:520px){.env-scale{transform:scale(.85);}}
/* ── THE ENVELOPE  300x200 ── */
.envelope{
  position:relative;
  background-color:#0c1e3a;
  width:300px;height:200px;
  cursor:pointer;z-index:5;
  outline:1.5px solid rgba(0,232,255,.2);
}
/* ── CARD ── */
.env-card{
  display:flex;justify-content:center;align-items:center;flex-direction:column;gap:8px;
  position:relative;top:10px;left:10px;
  background:linear-gradient(150deg,#e8f3ff,#d8ecff);
  width:280px;height:190px;border-radius:8px;z-index:1;
}
.env-close{
  position:absolute;top:10px;right:12px;
  font-family:'Orbitron',monospace;font-size:13px;font-weight:900;
  color:rgba(10,40,100,.45);background:none;border:none;outline:none;
  cursor:pointer;padding:2px 5px;line-height:1;z-index:20;transition:color .15s;
}
.env-close:hover{color:var(--hot);}
.env-qr-img{
  display:block;width:120px;height:120px;border-radius:3px;
  object-fit:contain;image-rendering:pixelated;image-rendering:crisp-edges;
}
.env-qr-fb{
  width:120px;height:120px;border-radius:4px;background:#fff;
  border:1.5px dashed rgba(0,80,200,.18);
  display:none;flex-direction:column;align-items:center;justify-content:center;
  text-align:center;padding:6px;
}
.env-qr-fb span{
  font-family:'Orbitron',monospace;font-size:.44rem;letter-spacing:.08em;color:rgba(10,40,100,.4);
}
.env-card-label{
  font-family:'Orbitron',monospace;font-size:.48rem;
  letter-spacing:.16em;text-transform:uppercase;color:rgba(10,40,100,.42);
}
/* ── TOP FLAP  z:10, clips to triangle, rotates on click ── */
.env-top{
  position:absolute;left:0;top:0;
  background-color:#0f2548;
  width:100%;height:65%;z-index:10;
  clip-path:polygon(0% 0%,50% 100%,100% 0%);
  box-shadow:inset 0 -2px 0 rgba(0,232,255,.15);
}
/* ── RIGHT / BOTTOM / LEFT  z:5 front panels ── */
.env-right{
  position:absolute;right:0;top:0;
  background-color:#091a32;
  width:50%;height:100%;z-index:5;
  clip-path:polygon(100% 0%,0% 51%,100% 100%);
}
.env-bottom{
  position:absolute;bottom:0;left:0;
  background-color:#071526;
  width:100%;height:50%;z-index:5;
  clip-path:polygon(0% 99%,50% 0%,100% 100%);
  border-bottom:1.5px solid rgba(0,232,255,.18);
}
.env-left{
  position:absolute;top:0;left:0;
  background-color:#091a32;
  width:50%;height:100%;z-index:5;
  clip-path:polygon(0% 0%,100% 50%,0% 101%);
}
.env-shadow{
  position:absolute;bottom:-100px;left:75px;
  width:150px;height:5px;border-radius:3px;
  box-shadow:10px 10px 7px rgba(0,232,255,.22);
}
/* ── CONTINUE BUTTON ── */
.env-cont-row{opacity:0;pointer-events:none;transition:opacity .35s;}
.env-cont-row.show{opacity:1;pointer-events:all;}
.env-cont-btn{
  padding:clamp(8px,1.3vh,11px) clamp(20px,3.5vw,34px);
  font-family:'Orbitron',monospace;
  font-size:clamp(.6rem,1.1vw,.76rem);font-weight:700;
  letter-spacing:.14em;text-transform:uppercase;
  border:1.5px solid rgba(0,232,255,.55);border-radius:3px;
  background:rgba(0,232,255,.08);color:var(--cyan);
  cursor:pointer;transition:all .22s var(--ease);
  clip-path:polygon(5px 0,100% 0,100% calc(100% - 5px),calc(100% - 5px) 100%,0 100%,0 5px);
}
.env-cont-btn:hover{
  background:rgba(0,232,255,.22);
  box-shadow:0 0 24px rgba(0,232,255,.45);
  transform:scale(1.05);
}
@media(hover:hover){.rk-ctrl{display:none}}
@media(hover:none){.rk-ctrl{display:flex}}
@media(max-width:380px){.fn-cnt{gap:8px}}
</style>
</head>
<body>

<!-- GLOBAL ELEMENTS -->
<div id="cur"></div>
<div id="cur-ring"></div>
<canvas id="bgc"></canvas>
<canvas id="bloom-overlay"></canvas>
<canvas id="burst-canvas"></canvas>

<!-- MISSION COUNTER -->
<div id="mission-ctr">MISSION <span id="mc-cur">01</span> / 06</div>

<!-- PROGRESS DOTS -->
<div class="pdots">
  <div class="pd active" data-i="0"></div>
  <div class="pd" data-i="1"></div>
  <div class="pd" data-i="2"></div>
  <div class="pd" data-i="3"></div>
  <div class="pd" data-i="4"></div>
  <div class="pd" data-i="5"></div>
  <span class="pd-label">JOSEPH · BDAY XI</span>
</div>

<!-- ═══ SCENE 0: INTRO ═══ -->
<section class="scene enter" id="s0">
  <div class="n-name">Joseph</div>
  <div class="n11 glitch">11</div>
  <div class="n-sub">Years &nbsp;·&nbsp; Birthday &nbsp;·&nbsp; Unlocked</div>
  <div class="tap-h">Tap to Begin</div>
</section>

<!-- ═══ SCENE 1: CAKE ═══ -->
<section class="scene" id="s1">
  <div class="sspot"></div><div class="sfloor"></div>
  <p class="wish">Make a Wish, Joseph…</p>
  <div class="c-stage">
    <div class="ck-w" id="ck-w">
      <div class="c-row" id="c-row"></div>
      <div class="ckL ckT"><div class="ck-fr"></div><div class="ck-fill"></div></div>
      <div class="ckL ckM"><div class="ck-fr"></div><div class="ck-fill"></div></div>
      <div class="ckL ckB"><div class="ck-fr"></div><div class="ck-fill"></div></div>
      <div class="ck-plate"></div>
    </div>
    <p class="ck-hint">Click flame to extinguish &nbsp;·&nbsp; Double-click cake to change theme</p>
    <div class="cake-actions">
      <button class="blow-btn" id="blow-btn">💨 &nbsp; BLOW ALL CANDLES</button>
    </div>
  </div>
</section>

<!-- ═══ SCENE 2: BALLOONS ═══ -->
<section class="scene" id="s2">
  <div class="bal-hdr">
    <div class="bal-title">POP THE BALLOONS</div>
    <div class="pop-ctr" id="pop-ctr">🎈 &nbsp; POPPED: 00</div>
  </div>
  <button class="nxt-btn" id="bal-nxt">CONTINUE &nbsp;→</button>
</section>

<!-- ═══ SCENE 3: FIREWORKS ═══ -->
<section class="scene" id="s3">
  <canvas id="fw-c"></canvas>
  <canvas id="fw-bloom"></canvas>
  <div class="fw-hdr">
    <div class="fw-ttl">✦ &nbsp; FIREWORKS &nbsp; ✦</div>
    <div class="fw-hnt">Click or drag to launch</div>
  </div>
  <button class="nxt-btn" id="fw-nxt" style="position:absolute;bottom:clamp(18px,3vh,26px);left:50%;transform:translateX(-50%);opacity:0;pointer-events:none">CONTINUE &nbsp;→</button>
</section>

<!-- ═══ SCENE 4: ROCKET ═══ -->
<section class="scene" id="s4">
  <canvas id="rk-c"></canvas>
  <canvas id="rk-bloom"></canvas>
  <div class="rk-hud hud-box" id="rk-hud">⭐ &nbsp; 0 / 20 &nbsp; STARS</div>
  <div class="mouse-guide" id="mouse-guide">
    🖱 &nbsp; MOVE MOUSE TO PILOT<br>
    <span style="font-size:.78em;opacity:.6">MOBILE: USE ARROW BUTTONS</span>
  </div>
  <div class="rk-ctrl">
    <button class="rk-btn" id="rk-l">◀</button>
    <button class="rk-btn" id="rk-r">▶</button>
  </div>
  <div class="win-ov" id="win-ov">
    <div class="win-ttl">MISSION COMPLETE!<br>⭐ 20 STARS ⭐</div>
    <button class="nxt-btn show" id="win-nxt" style="position:relative;bottom:auto;left:auto;transform:none;margin-top:26px">GRAND FINALE &nbsp;✦</button>
  </div>
</section>

<!-- ═══ SCENE 5: ENVELOPE / QR ═══ -->
<section class="scene" id="s5">
  <div class="env-wrapper">

    <div class="env-title">✦ &nbsp; A Special Delivery &nbsp; ✦</div>

    <div class="env-scale">
      <div class="envelope" id="envelope">

        <!-- Card: position:relative, starts inside, GSAP animates it out -->
        <div class="env-card" id="env-card">
          <button class="env-close" id="env-close">✕</button>
          <img class="env-qr-img" id="env-qr-img" src="qr.png" alt="QR Code">
          <div class="env-qr-fb" id="env-qr-fb">
            <span>Place qr.png<br>next to this file</span>
          </div>
          <div class="env-card-label">🚀 &nbsp; Scan Me, Joseph! &nbsp; 🚀</div>
        </div>

        <!-- Flap + side panels (exact structure from reference) -->
        <div class="env-top"    id="env-top"></div>
        <div class="env-right"></div>
        <div class="env-bottom"></div>
        <div class="env-left"></div>
        <div class="env-shadow" id="env-shadow"></div>

      </div>
    </div>

    <div class="env-cont-row" id="env-cont-row">
      <button class="env-cont-btn" id="env-cont-btn">Finale &nbsp;→</button>
    </div>

  </div>
</section>

<!-- ═══ SCENE 6: FINALE ═══ -->
<section class="scene" id="s6">
  <canvas id="fn-c"></canvas>
  <div class="fn-cnt">
    <div class="fn-badge">✦ &nbsp; BIRTHDAY PROTOCOL &nbsp; ✦</div>
    <div class="fn-name">Joseph</div>
    <div class="fn-ttl"><span>Happy 11th Birthday!</span></div>
    <div class="fn-num">11</div>
    <div class="fn-sub">Achievement Unlocked &nbsp;·&nbsp; Level XI</div>
    <button class="restart-btn" id="restart-btn">↺ &nbsp; RESTART MISSION</button>
  </div>
</section>

<script>
'use strict';

/* ═══════════════════════════════════════════════════
   CURSOR
═══════════════════════════════════════════════════ */
const CUR=document.getElementById('cur'), RING=document.getElementById('cur-ring');
let mx=innerWidth/2, my=innerHeight/2, rx=mx, ry=my;
document.addEventListener('mousemove', e=>{
  mx=e.clientX; my=e.clientY;
  CUR.style.left=mx+'px'; CUR.style.top=my+'px';
  if(Math.random()<.18){
    const s=document.createElement('div'); s.className='c-spark';
    const a=Math.random()*Math.PI*2, d=9+Math.random()*16;
    s.style.cssText=`left:${mx}px;top:${my}px;--dx:${(Math.cos(a)*d).toFixed(1)}px;--dy:${(Math.sin(a)*d).toFixed(1)}px;background:${['var(--gold)','var(--hot)','var(--cyan)','#fff'][Math.floor(Math.random()*4)]}`;
    document.body.appendChild(s); setTimeout(()=>s.remove(),560);
  }
});
(function rl(){rx+=(mx-rx)*.11;ry+=(my-ry)*.11;RING.style.left=rx+'px';RING.style.top=ry+'px';requestAnimationFrame(rl);})();
document.addEventListener('mousedown',()=>{CUR.style.transform='translate(-50%,-50%) scale(2.2)';CUR.style.mixBlendMode='normal'});
document.addEventListener('mouseup',()=>{CUR.style.transform='translate(-50%,-50%) scale(1)';CUR.style.mixBlendMode='screen'});

/* ═══════════════════════════════════════════════════
   BACKGROUND PARTICLES
═══════════════════════════════════════════════════ */
const bgc=document.getElementById('bgc'), bc=bgc.getContext('2d');
const blm=document.getElementById('bloom-overlay'), blmctx=blm.getContext('2d');
function rsz(){bgc.width=blm.width=innerWidth;bgc.height=blm.height=innerHeight;}
rsz(); addEventListener('resize',rsz);
const bgPts=Array.from({length:90},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.6+.3,vx:(Math.random()-.5)*.2,vy:(Math.random()-.5)*.2,h:Math.random()*360,ph:Math.random()*Math.PI*2}));
(function bgLoop(){
  bc.clearRect(0,0,bgc.width,bgc.height);
  bgPts.forEach(p=>{
    p.x+=p.vx; p.y+=p.vy; p.ph+=.009;
    if(p.x<0)p.x=bgc.width; if(p.x>bgc.width)p.x=0;
    if(p.y<0)p.y=bgc.height; if(p.y>bgc.height)p.y=0;
    const a=(Math.sin(p.ph)*.3+.55)*.62;
    bc.beginPath(); bc.arc(p.x,p.y,p.r,0,Math.PI*2);
    bc.fillStyle=`hsla(${p.h},72%,66%,${a})`; bc.fill();
  });
  // Bloom canvas mirrors bg with glow
  blmctx.clearRect(0,0,blm.width,blm.height);
  bgPts.forEach(p=>{
    blmctx.beginPath(); blmctx.arc(p.x,p.y,p.r*2.5,0,Math.PI*2);
    blmctx.fillStyle=`hsla(${p.h},100%,70%,.4)`; blmctx.fill();
  });
  requestAnimationFrame(bgLoop);
})();

/* ═══════════════════════════════════════════════════
   BURST CANVAS — Balloon shards
═══════════════════════════════════════════════════ */
const BRS=document.getElementById('burst-canvas'), BCTX=BRS.getContext('2d');
function rszBurst(){BRS.width=innerWidth;BRS.height=innerHeight;}
rszBurst(); addEventListener('resize',rszBurst);
let shards=[], airPuffs=[];

class RubberShard{
  constructor(cx,cy,col,vx,vy,sz){
    this.x=cx;this.y=cy;this.vx=vx;this.vy=vy;this.col=col;
    this.w=sz*(.55+Math.random()*.85);this.h=sz*(.22+Math.random()*.35);
    this.rot=Math.random()*Math.PI*2;this.spin=(Math.random()-.5)*.24;this.alpha=1;
    const n=4+Math.floor(Math.random()*3);this.pts=[];
    for(let i=0;i<n;i++){const a=(i/n)*Math.PI*2;this.pts.push({x:Math.cos(a)*this.w/2*(0.65+Math.random()*.7),y:Math.sin(a)*this.h/2*(0.65+Math.random()*.7)});}
  }
  tick(){this.vy+=.4;this.vx*=.97;this.vy*=.99;this.x+=this.vx;this.y+=this.vy;this.rot+=this.spin;this.alpha-=.015;}
  draw(ctx){
    if(this.alpha<=0)return;
    ctx.save();ctx.translate(this.x,this.y);ctx.rotate(this.rot);ctx.globalAlpha=Math.max(0,this.alpha);
    ctx.beginPath();ctx.moveTo(this.pts[0].x,this.pts[0].y);
    for(let i=1;i<this.pts.length;i++){const cp={x:(this.pts[i-1].x+this.pts[i].x)/2+((Math.random()-.5)*3),y:(this.pts[i-1].y+this.pts[i].y)/2+((Math.random()-.5)*3)};ctx.quadraticCurveTo(cp.x,cp.y,this.pts[i].x,this.pts[i].y);}
    ctx.closePath();ctx.fillStyle=this.col;ctx.shadowBlur=5;ctx.shadowColor=this.col;ctx.fill();
    ctx.beginPath();ctx.ellipse(this.pts[0].x*.28,this.pts[0].y*.28,this.w*.16,this.h*.1,Math.PI*.2,0,Math.PI*2);ctx.fillStyle='rgba(255,255,255,.38)';ctx.fill();
    ctx.restore();
  }
}
class AirPuff{
  constructor(x,y){const a=Math.random()*Math.PI*2,spd=1.4+Math.random()*3;this.x=x;this.y=y;this.vx=Math.cos(a)*spd;this.vy=Math.sin(a)*spd;this.r=2+Math.random()*4;this.alpha=.65;}
  tick(){this.x+=this.vx;this.y+=this.vy;this.vx*=.91;this.vy*=.91;this.r+=.28;this.alpha-=.038;}
  draw(ctx){ctx.save();ctx.globalAlpha=Math.max(0,this.alpha);ctx.beginPath();ctx.arc(this.x,this.y,this.r,0,Math.PI*2);ctx.fillStyle='rgba(255,255,255,.15)';ctx.fill();ctx.restore();}
}
(function bl(){
  BCTX.clearRect(0,0,BRS.width,BRS.height);
  shards=shards.filter(s=>s.alpha>0);shards.forEach(s=>{s.tick();s.draw(BCTX);});
  airPuffs=airPuffs.filter(p=>p.alpha>0);airPuffs.forEach(p=>{p.tick();p.draw(BCTX);});
  requestAnimationFrame(bl);
})();

function burstBalloon(cx,cy,col,col2,sz){
  const n=11+Math.floor(Math.random()*8);
  for(let i=0;i<n;i++){
    const a=(i/n)*Math.PI*2+(Math.random()-.5)*.55, spd=3.5+Math.random()*7.5;
    shards.push(new RubberShard(cx+(Math.random()-.5)*10,cy+(Math.random()-.5)*10,Math.random()<.5?col:col2,Math.cos(a)*spd,Math.sin(a)*spd-2.2,sz*(0.14+Math.random()*.2)));
  }
  for(let i=0;i<22;i++)airPuffs.push(new AirPuff(cx,cy));
  for(let i=0;i<18;i++){const a=Math.random()*Math.PI*2,spd=2+Math.random()*10;shards.push(new RubberShard(cx,cy,col,Math.cos(a)*spd,Math.sin(a)*spd-3,sz*.06));}
}

/* ═══════════════════════════════════════════════════
   SCENE MANAGER
═══════════════════════════════════════════════════ */
const SN=['s0','s1','s2','s3','s4','s5','s6'];
const MISSIONS=['01','02','03','04','05','06','06'];
let CS=0;

function goTo(idx){
  const from=document.getElementById(SN[CS]);
  from.classList.remove('enter');from.classList.add('exit');
  CS=idx;
  setTimeout(()=>{from.classList.remove('exit');from.style.opacity='0';},500);
  const to=document.getElementById(SN[idx]);
  to.style.opacity='0';to.classList.remove('exit','enter');
  setTimeout(()=>{to.classList.add('enter');onEnter(idx);updateNav();},360);
}
function nxt(){if(CS<SN.length-1)goTo(CS+1);}
function updateNav(){
  document.querySelectorAll('.pd').forEach(d=>{const i=+d.dataset.i;d.classList.toggle('active',i===CS);d.classList.toggle('done',i<CS);});
  document.getElementById('mc-cur').textContent=MISSIONS[Math.min(CS,MISSIONS.length-1)];
}
function onEnter(i){
  if(i===1)initCake();
  if(i===2)initBalloons();
  if(i===3)initFireworks();
  if(i===4)initRocket();
  if(i===5)initEnvelope();
  if(i===6)initFinale();
}

/* ═══════════════════════════════════════════════════
   HELPERS
═══════════════════════════════════════════════════ */
function ripple(x,y){
  const r=document.createElement('div');
  r.style.cssText=`position:fixed;left:${x}px;top:${y}px;width:0;height:0;border-radius:50%;border:2px solid var(--cyan);transform:translate(-50%,-50%);z-index:9990;animation:ripOut .48s ease-out forwards;pointer-events:none`;
  document.body.appendChild(r);setTimeout(()=>r.remove(),500);
}
document.head.insertAdjacentHTML('beforeend','<style>@keyframes ripOut{to{width:160px;height:160px;opacity:0}}</style>');

function confetti(n=140){
  const C=['#f5c518','#ff1a6c','#00e8ff','#8b2be2','#00ff88','#ff5a00','#fff','#ff88cc','#ffe57f'];
  for(let i=0;i<n;i++){
    const el=document.createElement('div');el.className='cfetti';
    const w=4+Math.random()*10,h=4+Math.random()*10;
    el.style.cssText=`left:${Math.random()*100}%;width:${w}px;height:${h}px;background:${C[i%C.length]};border-radius:${Math.random()<.28?'50%':'2px'};--d:${(1.5+Math.random()*2.2).toFixed(2)}s;--dl:${(Math.random()*.9).toFixed(2)}s;--r:${Math.floor(Math.random()*720)}deg;--x:${((Math.random()-.5)*330).toFixed(0)}px`;
    document.body.appendChild(el);setTimeout(()=>el.remove(),3800);
  }
}

function bigText(msg,col='var(--gold)'){
  const el=document.createElement('div');
  el.style.cssText=`position:fixed;inset:0;display:flex;align-items:center;justify-content:center;font-family:'Orbitron',monospace;font-size:clamp(1.2rem,5vw,3rem);font-weight:900;color:${col};text-shadow:0 0 35px ${col},0 0 70px ${col}55;text-align:center;padding:20px;z-index:600;pointer-events:none;animation:bigIn .65s var(--ease) forwards;letter-spacing:0.08em`;
  el.textContent=msg;document.body.appendChild(el);
  setTimeout(()=>{el.style.transition='opacity .55s';el.style.opacity='0';setTimeout(()=>el.remove(),580);},2100);
}

/* ═══════════════════════════════════════════════════
   SCENE 0 — INTRO
═══════════════════════════════════════════════════ */
const s0=document.getElementById('s0');
for(let i=0;i<4;i++){
  const ring=document.createElement('div');ring.className='i-ring';
  const sz=280+i*240;
  ring.style.cssText=`width:${sz}px;height:${sz}px;left:50%;top:50%;--t:${3+i*.85}s;--dl:${i*.75}s`;
  s0.appendChild(ring);
}
s0.addEventListener('click',e=>{ripple(e.clientX,e.clientY);setTimeout(nxt,230);});
s0.style.cursor='pointer';

/* ═══════════════════════════════════════════════════
   SCENE 1 — CAKE
═══════════════════════════════════════════════════ */
let cndlsBlown=0, cakeTheme=0;
const CAKE_T=[
  {t:'#c83270',m:'#dc6010',b:'#ac1850'},
  {t:'#6b3a2a',m:'#8b5a3c',b:'#4a2518'},
  {t:'#1a5fa0',m:'#0e8ca8',b:'#0c4478'},
  {t:'#7a2ba0',m:'#9a40b8',b:'#5a1880'},
];
const FROST_T=[
  ['#fff8ee','#fff0d0','#fff8ee'],
  ['#ffe4b5','#deb887','#f4a460'],
  ['#e0f4ff','#b8e8f8','#cce8ff'],
  ['#f5e0ff','#e8c8ff','#f0d8ff'],
];

// Candle wax melting state
let candleHeights=[58,58,58]; // px, start height
let waxDripCounts=[0,0,0];
let waxInterval=null;

function initCake(){
  cndlsBlown=0; candleHeights=[58,58,58]; waxDripCounts=[0,0,0];
  buildCandles(); addSprinkles(); addDrips();
  startWaxMelt();
}

function startWaxMelt(){
  if(waxInterval)clearInterval(waxInterval);
  waxInterval=setInterval(()=>{
    document.querySelectorAll('#c-row .candle').forEach((c,i)=>{
      const fw=document.getElementById('fw'+i);
      if(!fw||fw.classList.contains('out'))return; // only melt if burning
      // Shorten candle
      if(candleHeights[i]>28){
        candleHeights[i]-=0.6;
        const cb=c.querySelector('.cb');
        if(cb) cb.style.height=candleHeights[i]+'px';
      }
      // Accumulate drips
      const prob=0.12;
      if(Math.random()<prob){
        waxDripCounts[i]++;
        const col=['#ff9ab0','#b4e0ff','#b7e4c7'][i];
        const cb=c.querySelector('.cb');
        if(cb){
          const d=document.createElement('div');
          d.style.cssText=`position:absolute;bottom:-${8+waxDripCounts[i]*2}px;left:${20+Math.random()*45}%;width:${6+Math.random()*5}px;height:${8+Math.random()*10}px;background:${col};border-radius:0 0 50% 50%;z-index:3;opacity:0.85`;
          cb.appendChild(d);
        }
      }
    });
  },220);
}

function buildCandles(){
  const row=document.getElementById('c-row');row.innerHTML='';
  for(let i=0;i<3;i++){
    const c=document.createElement('div');c.className='candle';
    c.innerHTML=`
      <div class="fw" id="fw${i}">
        <div class="fglow"></div>
        <div class="fo"></div><div class="fm"></div><div class="fi"></div>
        <div class="c-smk" id="sm${i}">
          ${[0,1,2,3].map(j=>`<div class="smkp" style="--d:${.9+j*.26}s;--dl:${j*.09}s;--tx:${((Math.random()-.5)*24).toFixed(1)}px"></div>`).join('')}
        </div>
      </div>
      <div class="cb" style="height:58px"><div class="cwick"></div></div>`;
    c.addEventListener('click',()=>blowOne(i));
    c.addEventListener('touchstart',e=>{e.preventDefault();blowOne(i);},{passive:false});
    row.appendChild(c);
  }
}

function blowOne(i){
  const fw=document.getElementById('fw'+i);
  if(!fw||fw.classList.contains('out'))return;
  fw.classList.add('out');
  document.getElementById('sm'+i).querySelectorAll('.smkp').forEach(p=>p.classList.add('go'));
  cndlsBlown++;
  if(cndlsBlown>=3)setTimeout(allBlown,380);
}
function blowAll(){for(let i=0;i<3;i++)setTimeout(()=>blowOne(i),i*210);}
function allBlown(){
  clearInterval(waxInterval);
  confetti(210);bigText('🎂 HAPPY BIRTHDAY, JOSEPH! 🎂');
  setTimeout(nxt,3100);
}
document.getElementById('blow-btn').addEventListener('click',blowAll);
document.getElementById('ck-w').addEventListener('dblclick',()=>{
  cakeTheme=(cakeTheme+1)%CAKE_T.length;
  applyTheme();addSprinkles();addDrips();
  bigText(['PINK VELVET','CHOCOLATE FUDGE','OCEAN BLUE','LAVENDER DREAM'][cakeTheme],
          ['#ff9ab0','#c8a070','#00e5ff','#ce93d8'][cakeTheme]);
});

function applyTheme(){
  const th=CAKE_T[cakeTheme],fr=FROST_T[cakeTheme];
  document.querySelectorAll('#s1 .ck-fill')[0].style.background=`linear-gradient(180deg,${th.t},${th.t}99 45%,${th.t}77 100%)`;
  document.querySelectorAll('#s1 .ck-fill')[1].style.background=`linear-gradient(180deg,${th.m},${th.m}99 45%,${th.m}77 100%)`;
  document.querySelectorAll('#s1 .ck-fill')[2].style.background=`linear-gradient(180deg,${th.b},${th.b}99 45%,${th.b}77 100%)`;
  document.querySelectorAll('#s1 .ck-fr')[0].style.background=`linear-gradient(180deg,${fr[0]},${fr[0]}cc)`;
  document.querySelectorAll('#s1 .ck-fr')[1].style.background=`linear-gradient(180deg,${fr[1]},${fr[1]}cc)`;
  document.querySelectorAll('#s1 .ck-fr')[2].style.background=`linear-gradient(180deg,${fr[2]},${fr[2]}cc)`;
}
function addSprinkles(){
  const top=document.querySelector('#s1 .ckT .ck-fill');
  top.querySelectorAll('.spr').forEach(s=>s.remove());
  const C=['#ff1a6c','#00e8ff','#f5c518','#8b2be2','#00ff88','#ff5a00'];
  for(let i=0;i<22;i++){
    const s=document.createElement('div');s.className='spr';
    s.style.cssText=`position:absolute;height:4px;border-radius:2px;width:${10+Math.random()*10}px;left:${8+Math.random()*80}%;top:${10+Math.random()*65}%;background:${C[i%C.length]};transform:rotate(${Math.random()*170}deg);z-index:6`;
    top.appendChild(s);
  }
}
function addDrips(){
  ['.ckT','.ckM','.ckB'].forEach((sel,li)=>{
    const layer=document.querySelector('#s1 '+sel);
    layer.querySelectorAll('.drp').forEach(d=>d.remove());
    const n=[4,5,6][li],fr=FROST_T[cakeTheme][li];
    for(let j=0;j<n;j++){
      const d=document.createElement('div');d.className='drp';
      const w=9+Math.random()*9,h=13+Math.random()*14;
      d.style.cssText=`position:absolute;bottom:-${h-4}px;left:${5+j*(86/n)+Math.random()*4}%;width:${w}px;height:${h}px;background:${fr};border-radius:0 0 50% 50%;z-index:4`;
      layer.appendChild(d);
    }
  });
}

/* ═══════════════════════════════════════════════════
   SCENE 2 — BALLOONS
═══════════════════════════════════════════════════ */
const BCOLS=[['#ff1a6c','#ff85a1'],['#8b2be2','#d0a0f0'],['#00e8ff','#80f8ff'],['#f5c518','#ffe99a'],['#00ff88','#80ffba'],['#ff5a00','#ffb380'],['#e040fb','#f090ff'],['#2979ff','#82b1ff'],['#ff4444','#ff8a80']];
let popCount=0,balInt=null,balFrames={},nxtShown=false;

function initBalloons(){
  popCount=0;nxtShown=false;balFrames={};
  document.getElementById('pop-ctr').textContent='🎈  POPPED: 00';
  document.getElementById('bal-nxt').classList.remove('show');
  document.querySelectorAll('#s2 .balloon').forEach(b=>b.remove());
  for(let i=0;i<7;i++)setTimeout(spawnBalloon,i*260);
  balInt=setInterval(spawnBalloon,680);
}

function spawnBalloon(){
  const sc=document.getElementById('s2');
  const id='b'+Date.now()+Math.random().toString(36).slice(2);
  const [c1,c2]=BCOLS[Math.floor(Math.random()*BCOLS.length)];
  const sz=52+Math.random()*46;
  const x=4+Math.random()*88;
  const spd=.48+Math.random()*.88;
  const wobA=9+Math.random()*17,wobPh=Math.random()*Math.PI*2;

  const b=document.createElement('div');b.className='balloon';b.dataset.id=id;
  b.style.left=x+'%';

  // shadow element — positioned at bottom of scene
  const shadowEl=document.createElement('div');shadowEl.className='b-shadow';
  const shadowSz=sz*.6;
  shadowEl.style.cssText=`width:${shadowSz}px;height:${shadowSz*.3}px;background:radial-gradient(ellipse,rgba(0,0,0,.55),transparent 70%);position:fixed;bottom:0;left:${x}%;transform:translateX(-50%);border-radius:50%;pointer-events:none;z-index:3;transition:opacity .2s`;
  sc.appendChild(shadowEl);

  b.innerHTML=`
    <div class="b-bd" style="width:${sz}px;height:${sz*1.22}px;background:radial-gradient(circle at 34% 32%,${c2},${c1});box-shadow:inset -9px -11px 0 rgba(0,0,0,.2),0 6px 24px rgba(0,0,0,.4)"></div>
    <div class="b-kt" style="background:${c1}"></div>
    <div class="b-st"></div>`;
  sc.appendChild(b);

  let posY=window.innerHeight+10,t0=performance.now();
  const rise=now=>{
    const dt=now-t0;t0=now;
    posY-=spd;const wx=Math.sin(dt/700+wobPh)*wobA;
    b.style.transform=`translateY(${posY-window.innerHeight}px) translateX(${wx}px)`;
    // Scale shadow based on balloon height: far = small shadow, close to bottom = big
    const normY=Math.max(0,Math.min(1,(window.innerHeight-posY+200)/(window.innerHeight+200)));
    const shadowScale=0.3+normY*0.7;
    const shadowOpacity=0.15+normY*0.5;
    shadowEl.style.transform=`translateX(calc(-50% + ${wx}px)) scaleX(${shadowScale})`;
    shadowEl.style.opacity=shadowOpacity;
    if(posY<-240){b.remove();shadowEl.remove();delete balFrames[id];return;}
    balFrames[id]=requestAnimationFrame(rise);
  };
  balFrames[id]=requestAnimationFrame(rise);

  const pop=e=>{
    if(balFrames[id])cancelAnimationFrame(balFrames[id]);
    delete balFrames[id];
    const rect=b.getBoundingClientRect();
    burstBalloon(rect.left+rect.width/2,rect.top+rect.height/2,c1,c2,sz);
    b.remove();shadowEl.remove();
    popCount++;
    document.getElementById('pop-ctr').textContent=`🎈  POPPED: ${String(popCount).padStart(2,'0')}`;
    if(popCount===10)bigText('🔥  ON FIRE, JOSEPH!','var(--hot)');
    if(popCount>=15&&!nxtShown){nxtShown=true;document.getElementById('bal-nxt').classList.add('show');}
  };
  b.addEventListener('click',pop);
  b.addEventListener('touchstart',e=>{e.preventDefault();pop(e);},{passive:false});
}

document.getElementById('bal-nxt').addEventListener('click',()=>{
  clearInterval(balInt);
  Object.values(balFrames).forEach(cancelAnimationFrame);
  document.querySelectorAll('#s2 .balloon,.b-shadow').forEach(b=>b.remove());
  nxt();
});

/* ═══════════════════════════════════════════════════
   SCENE 3 — FIREWORKS
═══════════════════════════════════════════════════ */
let fwActive=false,fwParts=[],fwRkts=[],fwRAF;

class FwRkt{
  constructor(x){this.x=x;this.y=innerHeight;this.ty=innerHeight*(.08+Math.random()*.45);this.prog=0;this.trail=[];this.done=false;this.col=`hsl(${Math.floor(Math.random()*360)},100%,65%)`;}
  tick(){this.prog=Math.min(1,this.prog+.04);const e=1-Math.pow(1-this.prog,3);this.cy=innerHeight+(this.ty-innerHeight)*e;this.trail.push({x:this.x,y:this.cy});if(this.trail.length>18)this.trail.shift();if(this.prog>=1){this.done=true;fwBurst(this.x,this.ty,this.col);}}
  draw(ctx){this.trail.forEach((p,i)=>{const a=(i/this.trail.length)*.9,r=(i/this.trail.length)*3.5;ctx.beginPath();ctx.arc(p.x,p.y,r,0,Math.PI*2);ctx.fillStyle=this.col;ctx.globalAlpha=a;ctx.shadowBlur=8;ctx.shadowColor=this.col;ctx.fill();});ctx.globalAlpha=1;}
}
class FwSp{
  constructor(x,y,col){const a=Math.random()*Math.PI*2,spd=1.5+Math.random()*6;this.x=x;this.y=y;this.vx=Math.cos(a)*spd;this.vy=Math.sin(a)*spd;this.col=col;this.a=1;this.r=1.5+Math.random()*2.5;this.tail=[];}
  tick(){this.tail.push({x:this.x,y:this.y});if(this.tail.length>6)this.tail.shift();this.vx*=.965;this.vy=this.vy*.965+.09;this.x+=this.vx;this.y+=this.vy;this.a-=.013;}
  draw(ctx){ctx.globalAlpha=Math.max(0,this.a);if(this.tail.length>1){ctx.beginPath();ctx.moveTo(this.tail[0].x,this.tail[0].y);this.tail.forEach(p=>ctx.lineTo(p.x,p.y));ctx.strokeStyle=this.col;ctx.lineWidth=this.r*.5;ctx.globalAlpha=Math.max(0,this.a*.45);ctx.shadowBlur=4;ctx.shadowColor=this.col;ctx.stroke();}ctx.globalAlpha=Math.max(0,this.a);ctx.beginPath();ctx.arc(this.x,this.y,this.r,0,Math.PI*2);ctx.fillStyle=this.col;ctx.shadowBlur=10;ctx.shadowColor=this.col;ctx.fill();ctx.globalAlpha=1;}
}
function fwBurst(x,y,col){
  const n=65+Math.floor(Math.random()*40);
  const col2=`hsl(${(parseInt(col.match(/\d+/)[0])+140)%360},100%,72%)`;
  if(Math.random()<.18){make11(x,y);return;}
  for(let i=0;i<n;i++){const sp=new FwSp(x,y,i%4===0?col2:col);const a=(i/n)*Math.PI*2,spd=1.8+Math.random()*5.5;sp.vx=Math.cos(a)*spd;sp.vy=Math.sin(a)*spd;fwParts.push(sp);}
}
function make11(cx,cy){
  const pts=[];for(let i=0;i<10;i++){pts.push({x:-20,y:i*9-40});pts.push({x:20,y:i*9-40});}
  pts.push({x:-26,y:-40},{x:-12,y:-40},{x:12,y:-40},{x:26,y:-40});
  pts.forEach(p=>{const sp=new FwSp(cx+p.x,cy+p.y,'#fff');sp.vx=(Math.random()-.5)*.4;sp.vy=(Math.random()-.5)*.4;sp.r=3;sp.a=1.2;fwParts.push(sp);});
}

function initFireworks(){
  const cv=document.getElementById('fw-c'),ctx=cv.getContext('2d');
  const blmCv=document.getElementById('fw-bloom'),blmCtx=blmCv.getContext('2d');
  cv.width=blmCv.width=innerWidth;cv.height=blmCv.height=innerHeight;
  fwActive=true;fwParts=[];fwRkts=[];
  const nb=document.getElementById('fw-nxt');nb.style.opacity='0';nb.style.pointerEvents='none';
  [.28,.72,.5,.15,.85].forEach((x,i)=>setTimeout(()=>fwRkts.push(new FwRkt(innerWidth*x)),350+i*450));
  setTimeout(()=>{nb.style.opacity='1';nb.style.pointerEvents='all';nb.classList.add('show');},5500);
  cv.onclick=e=>{if(fwActive)fwRkts.push(new FwRkt(e.offsetX));};
  let lX=-999;cv.onmousemove=e=>{if(e.buttons&&Math.abs(e.offsetX-lX)>25){lX=e.offsetX;fwRkts.push(new FwRkt(e.offsetX));}};
  cv.addEventListener('touchmove',e=>{const t=e.touches[0];fwRkts.push(new FwRkt(t.clientX));},{passive:true});
  (function fwLoop(){
    if(!fwActive)return;
    ctx.fillStyle='rgba(0,0,0,.16)';ctx.fillRect(0,0,cv.width,cv.height);
    fwRkts=fwRkts.filter(r=>!r.done);fwRkts.forEach(r=>{r.tick();r.draw(ctx);});
    fwParts=fwParts.filter(s=>s.a>0);fwParts.forEach(s=>{s.tick();s.draw(ctx);});
    // Bloom mirror
    blmCtx.clearRect(0,0,blmCv.width,blmCv.height);
    fwParts.forEach(s=>{blmCtx.globalAlpha=Math.max(0,s.a*.6);blmCtx.beginPath();blmCtx.arc(s.x,s.y,s.r*2.5,0,Math.PI*2);blmCtx.fillStyle=s.col;blmCtx.fill();blmCtx.globalAlpha=1;});
    fwRAF=requestAnimationFrame(fwLoop);
  })();
}
document.getElementById('fw-nxt').addEventListener('click',()=>{fwActive=false;cancelAnimationFrame(fwRAF);nxt();});

/* ═══════════════════════════════════════════════════
   SCENE 4 — ROCKET GAME
═══════════════════════════════════════════════════ */
let rkActive=false,rkRAF,rkKeys={},rkStars=[],rkCount=0;
// Parallax star layers
let rkLayer1=[],rkLayer2=[],rkLayer3=[];
let rkMouseX=innerWidth/2;
// Smoke trail particles
let smokeParts=[];

class SmokePart{
  constructor(x,y,vx){
    this.x=x+((Math.random()-.5)*8);this.y=y;
    this.vx=vx*.12+(Math.random()-.5)*.4;this.vy=1.2+Math.random()*1.5;
    this.r=4+Math.random()*6;this.alpha=0.35+Math.random()*.25;
    this.life=1;
  }
  tick(){this.x+=this.vx;this.y+=this.vy;this.r+=0.18;this.alpha-=0.008;this.life-=0.008;}
  draw(ctx){
    if(this.alpha<=0)return;
    ctx.save();ctx.globalAlpha=Math.max(0,this.alpha);
    const g=ctx.createRadialGradient(this.x,this.y,0,this.x,this.y,this.r);
    g.addColorStop(0,'rgba(180,180,180,0.6)');g.addColorStop(1,'rgba(100,100,100,0)');
    ctx.fillStyle=g;ctx.beginPath();ctx.arc(this.x,this.y,this.r,0,Math.PI*2);ctx.fill();
    ctx.restore();
  }
}

function initRocket(){
  rkActive=true;rkCount=0;rkStars=[];smokeParts=[];
  document.getElementById('rk-hud').textContent='⭐  0 / 20  STARS';
  document.getElementById('win-ov').classList.remove('show');
  const cv=document.getElementById('rk-c'),ctx=cv.getContext('2d');
  const blmCv=document.getElementById('rk-bloom'),blmCtx=blmCv.getContext('2d');
  cv.width=blmCv.width=innerWidth;cv.height=blmCv.height=innerHeight;
  const W=cv.width,H=cv.height;

  rkMouseX=W/2;

  // === PARALLAX STAR LAYERS ===
  // Layer 1: far — tiny, very slow
  rkLayer1=Array.from({length:180},()=>({x:Math.random()*W,y:Math.random()*H,r:Math.random()*.8+.15,ph:Math.random()*Math.PI*2,spd:.04+Math.random()*.08}));
  // Layer 2: mid — medium, medium speed
  rkLayer2=Array.from({length:80},()=>({x:Math.random()*W,y:Math.random()*H,r:Math.random()*1.2+.4,ph:Math.random()*Math.PI*2,spd:.18+Math.random()*.22}));
  // Layer 3: near — bright, fast
  rkLayer3=Array.from({length:30},()=>({x:Math.random()*W,y:Math.random()*H,r:Math.random()*2+.8,ph:Math.random()*Math.PI*2,spd:.6+Math.random()*.6}));

  const rk={x:W/2,y:H-90,vx:0,vy:0,w:28,h:60,tilt:0};

  const s4=document.getElementById('s4');
  const onMM=e=>{const rect=cv.getBoundingClientRect();rkMouseX=e.clientX-rect.left;};
  const onTM=e=>{e.preventDefault();const rect=cv.getBoundingClientRect();rkMouseX=e.touches[0].clientX-rect.left;};
  s4.addEventListener('mousemove',onMM);
  cv.addEventListener('touchmove',onTM,{passive:false});
  const kd=e=>rkKeys[e.code]=true,ku=e=>rkKeys[e.code]=false;
  addEventListener('keydown',kd);addEventListener('keyup',ku);
  const rkL=document.getElementById('rk-l'),rkR=document.getElementById('rk-r');
  rkL.addEventListener('pointerdown',()=>rkKeys['ArrowLeft']=true);rkL.addEventListener('pointerup',()=>rkKeys['ArrowLeft']=false);rkL.addEventListener('pointercancel',()=>rkKeys['ArrowLeft']=false);
  rkR.addEventListener('pointerdown',()=>rkKeys['ArrowRight']=true);rkR.addEventListener('pointerup',()=>rkKeys['ArrowRight']=false);rkR.addEventListener('pointercancel',()=>rkKeys['ArrowRight']=false);

  let spawnT=0,thrBoost=0;

  function rkLoop(t){
    if(!rkActive)return;
    ctx.clearRect(0,0,W,H);blmCtx.clearRect(0,0,W,H);

    // BG gradient
    const bg=ctx.createLinearGradient(0,0,0,H);
    bg.addColorStop(0,'#000012');bg.addColorStop(.4,'#030022');bg.addColorStop(1,'#08002e');
    ctx.fillStyle=bg;ctx.fillRect(0,0,W,H);

    // Nebula clouds
    ctx.save();
    let nb=ctx.createRadialGradient(W*.22,H*.28,0,W*.22,H*.28,H*.52);
    nb.addColorStop(0,'rgba(70,0,160,.11)');nb.addColorStop(1,'transparent');
    ctx.fillStyle=nb;ctx.fillRect(0,0,W,H);
    nb=ctx.createRadialGradient(W*.78,H*.62,0,W*.78,H*.62,H*.38);
    nb.addColorStop(0,'rgba(0,80,180,.09)');nb.addColorStop(1,'transparent');
    ctx.fillStyle=nb;ctx.fillRect(0,0,W,H);
    nb=ctx.createRadialGradient(W*.5,H*.45,0,W*.5,H*.45,H*.3);
    nb.addColorStop(0,'rgba(140,0,80,.06)');nb.addColorStop(1,'transparent');
    ctx.fillStyle=nb;ctx.fillRect(0,0,W,H);
    ctx.restore();

    // === PARALLAX LAYERS ===
    // Layer 1 far — dim, slow
    rkLayer1.forEach(s=>{
      s.ph+=.008;s.y+=s.spd;if(s.y>H){s.y=-5;s.x=Math.random()*W;}
      const a=(Math.sin(s.ph)*.2+.4)*.55;
      ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);
      ctx.fillStyle=`rgba(200,220,255,${a})`;ctx.fill();
    });
    // Layer 2 mid — medium
    rkLayer2.forEach(s=>{
      s.ph+=.012;s.y+=s.spd;if(s.y>H){s.y=-5;s.x=Math.random()*W;}
      const a=(Math.sin(s.ph)*.28+.55)*.75;
      ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);
      ctx.fillStyle=`rgba(255,255,255,${a})`;ctx.fill();
    });
    // Layer 3 near — bright, fast, occasional color tint
    rkLayer3.forEach(s=>{
      s.ph+=.02;s.y+=s.spd;if(s.y>H){s.y=-5;s.x=Math.random()*W;}
      const a=(Math.sin(s.ph)*.35+.6)*.9;
      ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);
      ctx.fillStyle=`rgba(255,255,255,${a})`;ctx.fill();
      // Occasional colored near-stars for depth
      if(s.r>1.8){ctx.shadowBlur=5;ctx.shadowColor='rgba(0,232,255,.5)';ctx.fill();ctx.shadowBlur=0;}
    });

    // Move rocket
    let targetX=rkMouseX;
    if(rkKeys['ArrowLeft']||rkKeys['KeyA'])targetX=rk.x-120;
    if(rkKeys['ArrowRight']||rkKeys['KeyD'])targetX=rk.x+120;
    const dx=targetX-rk.x;
    rk.vx+=dx*.055;rk.vx*=0.82;rk.x+=rk.vx;
    rk.x=Math.max(rk.w,Math.min(W-rk.w,rk.x));
    rk.y=Math.max(H*.5,Math.min(H-rk.h*.5,rk.y));
    const targetTilt=rk.vx*.055;rk.tilt+=(targetTilt-rk.tilt)*.14;
    thrBoost=Math.min(1,Math.abs(rk.vx)/6);

    // === SMOKE TRAIL ===
    if(Math.random()<.45){
      smokeParts.push(new SmokePart(rk.x,rk.y+rk.h*.42+8,rk.vx));
    }
    smokeParts=smokeParts.filter(s=>s.life>0);
    smokeParts.forEach(s=>{s.tick();s.draw(ctx);});

    // Spawn stars
    spawnT++;
    if(spawnT%52===0&&rkStars.length<12){
      rkStars.push({x:28+Math.random()*(W-56),y:-35,vy:.7+Math.random()*1.3,sz:16+Math.random()*13,ph:Math.random()*Math.PI*2});
    }

    // Draw stars
    rkStars.forEach((s,i)=>{
      s.y+=s.vy;s.ph+=.045;
      if(s.y>H+60){rkStars.splice(i,1);return;}
      ctx.save();ctx.translate(s.x,s.y);
      ctx.shadowBlur=18+Math.sin(s.ph)*7;ctx.shadowColor='gold';
      ctx.fillStyle=`rgba(255,215,0,${.82+Math.sin(s.ph)*.18})`;
      drawStar(ctx,0,0,5,s.sz/2,s.sz/4.2);
      // Star glow ring
      ctx.beginPath();ctx.arc(0,0,s.sz/2+5+Math.sin(s.ph)*3,0,Math.PI*2);
      ctx.strokeStyle=`rgba(255,215,0,${.14+Math.sin(s.ph)*.08})`;ctx.lineWidth=1.5;ctx.stroke();
      // Bloom for star
      blmCtx.save();blmCtx.globalAlpha=.5+Math.sin(s.ph)*.2;
      blmCtx.beginPath();blmCtx.arc(s.x,s.y,s.sz,0,Math.PI*2);
      blmCtx.fillStyle='rgba(255,215,0,.8)';blmCtx.fill();blmCtx.restore();
      ctx.restore();

      // Collision
      const cdx=Math.abs(s.x-rk.x),cdy=Math.abs(s.y-rk.y);
      if(cdx<rk.w+s.sz*.4&&cdy<rk.h*.5+s.sz*.4){
        rkStars.splice(i,1);rkCount++;
        document.getElementById('rk-hud').textContent=`⭐  ${rkCount} / 20  STARS`;
        ctx.save();ctx.globalAlpha=.22;ctx.fillStyle='gold';ctx.fillRect(0,0,W,H);ctx.restore();
        if(rkCount>=20){rkActive=false;setTimeout(winRocket,380);}
      }
    });

    drawRocket(ctx,blmCtx,rk.x,rk.y,rk.tilt,rk.vx,thrBoost,t);
    rkRAF=requestAnimationFrame(rkLoop);
  }
  rkRAF=requestAnimationFrame(rkLoop);

  document.getElementById('win-nxt').onclick=()=>{
    rkActive=false;cancelAnimationFrame(rkRAF);
    removeEventListener('keydown',kd);removeEventListener('keyup',ku);
    s4.removeEventListener('mousemove',onMM);
    cv.removeEventListener('touchmove',onTM);
    nxt();
  };
}

function drawStar(ctx,cx,cy,spikes,oR,iR){
  let rot=-Math.PI/2,step=Math.PI/spikes;
  ctx.beginPath();ctx.moveTo(cx+Math.cos(rot)*oR,cy+Math.sin(rot)*oR);
  for(let i=0;i<spikes;i++){rot+=step;ctx.lineTo(cx+Math.cos(rot)*iR,cy+Math.sin(rot)*iR);rot+=step;ctx.lineTo(cx+Math.cos(rot)*oR,cy+Math.sin(rot)*oR);}
  ctx.closePath();ctx.fill();
}

function drawRocket(ctx,blmCtx,x,y,tilt,vx,boost,t){
  ctx.save();ctx.translate(x,y);ctx.rotate(tilt);

  // THRUSTER FLAME
  const fl=22+Math.sin(t/68)*9+boost*26;
  ctx.save();
  const fg1=ctx.createLinearGradient(0,28,0,28+fl);
  fg1.addColorStop(0,'rgba(255,215,60,1)');fg1.addColorStop(.32,'rgba(255,120,0,.92)');fg1.addColorStop(1,'rgba(255,40,0,0)');
  ctx.fillStyle=fg1;ctx.shadowBlur=32+boost*20;ctx.shadowColor='#ff9500';
  ctx.beginPath();ctx.ellipse(0,28+fl*.48,11+boost*7,fl*.52,0,0,Math.PI*2);ctx.fill();
  const fg2=ctx.createLinearGradient(0,28,0,28+fl*.55);
  fg2.addColorStop(0,'rgba(255,255,200,1)');fg2.addColorStop(1,'rgba(255,200,0,0)');
  ctx.fillStyle=fg2;ctx.beginPath();ctx.ellipse(0,28+fl*.27,5,fl*.3,0,0,Math.PI*2);ctx.fill();
  if(boost>.28){const sf=fl*.38;ctx.globalAlpha=boost*.65;ctx.beginPath();ctx.ellipse(-9,30+sf*.4,3.5,sf*.32,-.32,0,Math.PI*2);ctx.fill();ctx.beginPath();ctx.ellipse(9,30+sf*.4,3.5,sf*.32,.32,0,Math.PI*2);ctx.fill();}
  ctx.restore();
  // Bloom: flame
  blmCtx.save();blmCtx.translate(x,y);blmCtx.rotate(tilt);
  blmCtx.globalAlpha=(0.4+boost*.5);
  const bfg=blmCtx.createRadialGradient(0,28+fl*.4,0,0,28+fl*.4,fl*.8);
  bfg.addColorStop(0,'rgba(255,150,0,.9)');bfg.addColorStop(1,'rgba(255,100,0,0)');
  blmCtx.fillStyle=bfg;blmCtx.beginPath();blmCtx.ellipse(0,28+fl*.4,fl*.6,fl*.55,0,0,Math.PI*2);blmCtx.fill();
  blmCtx.restore();

  // BODY
  ctx.shadowBlur=22;ctx.shadowColor='rgba(0,200,255,.3)';
  const bG=ctx.createLinearGradient(-15,0,15,0);
  bG.addColorStop(0,'#37474f');bG.addColorStop(.28,'#90a4ae');bG.addColorStop(.52,'#eceff1');bG.addColorStop(.78,'#b0bec5');bG.addColorStop(1,'#263238');
  ctx.fillStyle=bG;ctx.beginPath();ctx.roundRect(-14,-22,28,52,5);ctx.fill();
  ctx.fillStyle='rgba(255,255,255,.08)';ctx.beginPath();ctx.roundRect(-4,-20,8,48,3);ctx.fill();

  // NOSE
  const nG=ctx.createLinearGradient(-12,-55,12,-22);
  nG.addColorStop(0,'#e53935');nG.addColorStop(.38,'#ff7043');nG.addColorStop(1,'#b71c1c');
  ctx.fillStyle=nG;ctx.shadowColor='rgba(255,60,0,.4)';ctx.shadowBlur=14;
  ctx.beginPath();ctx.moveTo(0,-55);ctx.bezierCurveTo(7,-38,13,-28,14,-22);ctx.lineTo(-14,-22);ctx.bezierCurveTo(-13,-28,-7,-38,0,-55);ctx.fill();
  ctx.strokeStyle='rgba(255,255,255,.12)';ctx.lineWidth=1;
  ctx.beginPath();ctx.moveTo(0,-55);ctx.bezierCurveTo(-2,-44,-1,-33,0,-22);ctx.stroke();

  // PORTHOLE
  ctx.shadowBlur=18;ctx.shadowColor='#00e8ff';
  const wG=ctx.createRadialGradient(-2,-4,1,0,-2,10);
  wG.addColorStop(0,'#80deea');wG.addColorStop(.5,'#00acc1');wG.addColorStop(1,'#006064');
  ctx.fillStyle=wG;ctx.beginPath();ctx.arc(0,-2,10,0,Math.PI*2);ctx.fill();
  ctx.strokeStyle='rgba(255,255,255,.32)';ctx.lineWidth=1.5;ctx.beginPath();ctx.arc(0,-2,10,0,Math.PI*2);ctx.stroke();
  ctx.fillStyle='rgba(255,255,255,.55)';ctx.beginPath();ctx.arc(-3,-5,4,0,Math.PI*2);ctx.fill();
  ctx.fillStyle='rgba(255,255,255,.2)';ctx.beginPath();ctx.arc(2,-1,2.5,0,Math.PI*2);ctx.fill();
  // Porthole bloom
  blmCtx.save();blmCtx.translate(x,y);blmCtx.rotate(tilt);
  blmCtx.globalAlpha=.55;blmCtx.beginPath();blmCtx.arc(0,-2,18,0,Math.PI*2);
  blmCtx.fillStyle='rgba(0,232,255,.7)';blmCtx.fill();blmCtx.restore();

  // FINS
  ctx.shadowBlur=0;
  const fG=ctx.createLinearGradient(-14,0,14,0);fG.addColorStop(0,'#546e7a');fG.addColorStop(1,'#37474f');ctx.fillStyle=fG;
  ctx.beginPath();ctx.moveTo(14,8);ctx.lineTo(27,32);ctx.lineTo(22,32);ctx.lineTo(14,26);ctx.closePath();ctx.fill();
  ctx.beginPath();ctx.moveTo(-14,8);ctx.lineTo(-27,32);ctx.lineTo(-22,32);ctx.lineTo(-14,26);ctx.closePath();ctx.fill();
  ctx.strokeStyle='rgba(255,255,255,.08)';ctx.lineWidth=1;
  ctx.beginPath();ctx.moveTo(14,8);ctx.lineTo(27,32);ctx.stroke();
  ctx.beginPath();ctx.moveTo(-14,8);ctx.lineTo(-27,32);ctx.stroke();

  // RACING STRIPES
  const sh=Math.floor(t/24)%360;
  ctx.fillStyle=`hsla(${sh},90%,65%,.9)`;ctx.shadowBlur=9;ctx.shadowColor=`hsl(${sh},90%,65%)`;
  ctx.beginPath();ctx.roundRect(-17,-8,5,28,2);ctx.fill();
  ctx.beginPath();ctx.roundRect(12,-8,5,28,2);ctx.fill();

  // RIVETS
  ctx.shadowBlur=0;ctx.fillStyle='rgba(255,255,255,.28)';
  [-11,11].forEach(sx=>{ctx.beginPath();ctx.arc(sx,-18,1.4,0,Math.PI*2);ctx.fill();});
  [-11,11].forEach(sx=>{ctx.beginPath();ctx.arc(sx,22,1.4,0,Math.PI*2);ctx.fill();});

  ctx.restore();
}

function winRocket(){confetti(140);document.getElementById('win-ov').classList.add('show');}

/* ═══════════════════════════════════════════════════
/* ═══════════════════════════════════════════════════
   SCENE 5 — ENVELOPE  (exact port of reference code)
   Reference: gsap timeline t1+t2, jQuery-style click
═══════════════════════════════════════════════════ */
function initEnvelope(){

  /* ── QR image / fallback ───────────────────────── */
  const qrImg = document.getElementById('env-qr-img');
  const qrFb  = document.getElementById('env-qr-fb');
  // show fallback if image failed (works for re-entries too)
  const showFb = () => { qrImg.style.display='none'; qrFb.style.display='flex'; };
  qrImg.onerror = showFb;
  if(qrImg.complete && qrImg.naturalWidth === 0){ showFb(); }

  /* ── Reset cont-row ────────────────────────────── */
  const contRow = document.getElementById('env-cont-row');
  contRow.classList.remove('show');

  /* ── Kill any running timelines from a previous visit ── */
  if(window._envT1){ window._envT1.kill(); window._envT1 = null; }
  if(window._envT2){ window._envT2.kill(); window._envT2 = null; }

  /* ── Reset all elements to initial closed state ── */
  gsap.set('#env-top',    { rotateX:0, zIndex:10, clearProps:'borderTop' });
  gsap.set('#env-card',   { zIndex:1, left:10, top:10, width:280, height:190, scaleX:1, scaleY:1 });
  gsap.set('#env-shadow', { scaleX:1 });

  /* ── Build timelines EXACTLY as reference ────────
     t1: flap opens → card shrinks → card flies up → card expands
     t2: shadow stretches (delayed)
  ─────────────────────────────────────────────── */
  const t1 = gsap.timeline({ paused: true });
  const t2 = gsap.timeline({ paused: true });

  t1
    .to('#env-top', {
      duration: 0.8,
      ease: 'back',
      transformOrigin: 'top',
      rotateX: 180,
      borderTop: '1px solid rgba(0,232,255,0.35)',
      zIndex: 1
    })
    .set('#env-card', { zIndex: 2 })
    .to('#env-card', {
      duration: 0.3,
      left: 30,
      top: 30,
      width: 250,
      height: 150
    })
    .to('#env-card', {
      top: -220
    })
    .set('#env-card', { zIndex: 10 })
    .to('#env-card', {
      duration: 0.7,
      scaleX: 1.4,
      scaleY: 1.6,
      top: 10,
      ease: 'back',
      onComplete: () => { contRow.classList.add('show'); }
    });

  t2.to('#env-shadow', {
    delay: 1.6,
    duration: 0.7,
    scaleX: 2.1
  });

  window._envT1 = t1;
  window._envT2 = t2;

  /* ── Click envelope to OPEN (remove old listener first) ── */
  const envelope = document.getElementById('envelope');
  // Clone node to wipe all old listeners, then re-append children
  // (simpler: just use a named handler stored on the element)
  if(envelope._envClickHandler){
    envelope.removeEventListener('click', envelope._envClickHandler);
  }
  envelope._envClickHandler = () => { t1.play(); t2.play(); };
  envelope.addEventListener('click', envelope._envClickHandler);

  /* ── Click ✕ to CLOSE ── */
  document.getElementById('env-close').onclick = (e) => {
    e.stopPropagation();
    contRow.classList.remove('show');
    t1.reverse();
    t2.reverse();
  };

  /* ── Continue → finale ── */
  document.getElementById('env-cont-btn').onclick = () => nxt();
}
let fnActive=false,fnRAF,fnParts=[];

function initFinale(){
  fnActive=true;fnParts=[];
  const cv=document.getElementById('fn-c'),ctx=cv.getContext('2d');
  cv.width=innerWidth;cv.height=innerHeight;
  confetti(320);

  const autoBurst=()=>{
    if(!fnActive)return;
    const x=innerWidth*(.08+Math.random()*.84),y=innerHeight*(.04+Math.random()*.52),hue=Math.random()*360;
    for(let i=0;i<85;i++){const a=(i/85)*Math.PI*2,spd=2+Math.random()*5.5;fnParts.push({x,y,vx:Math.cos(a)*spd,vy:Math.sin(a)*spd,a:1,col:`hsl(${hue+i%4*30},100%,65%)`,r:1.5+Math.random()*2.5});}
    setTimeout(autoBurst,350+Math.random()*520);
  };
  setTimeout(autoBurst,150);

  let rkX=-80,rkT=[];
  const rkA=()=>{if(!fnActive)return;rkX=-80;rkT=[];const d=()=>{if(!fnActive)return;rkX+=5;rkT.push({x:rkX,y:innerHeight*.12});if(rkX<innerWidth+80)requestAnimationFrame(d);};requestAnimationFrame(d);setTimeout(rkA,3100);};
  setTimeout(rkA,400);

  const bBg=[];const BC=['#ff1a6c','#8b2be2','#00e8ff','#f5c518','#00ff88','#ff5a00'];
  for(let i=0;i<14;i++)bBg.push({x:5+Math.random()*90,y:110+Math.random()*60,spd:.28+Math.random()*.5,sz:28+Math.random()*24,col:BC[i%BC.length]});

  (function fnLoop(){
    if(!fnActive)return;
    ctx.fillStyle='rgba(0,0,0,.11)';ctx.fillRect(0,0,cv.width,cv.height);
    const W=cv.width,H=cv.height;
    bBg.forEach(b=>{b.y-=b.spd;if(b.y<-100)b.y=H+50;ctx.save();ctx.globalAlpha=.3;const g=ctx.createRadialGradient(b.x/100*W-b.sz/3,H-b.y-b.sz/3,2,b.x/100*W,H-b.y,b.sz);g.addColorStop(0,b.col+'ff');g.addColorStop(1,b.col+'88');ctx.fillStyle=g;ctx.beginPath();ctx.ellipse(b.x/100*W,H-b.y,b.sz,b.sz*1.2,0,0,Math.PI*2);ctx.fill();ctx.restore();});
    rkT.forEach((p,i)=>{if(i%8<4){ctx.save();ctx.globalAlpha=(i/rkT.length)*.85;ctx.font=`bold 22px 'Orbitron',monospace`;ctx.fillStyle='gold';ctx.shadowBlur=14;ctx.shadowColor='gold';ctx.fillText('1',p.x-9,p.y);ctx.fillText('1',p.x+9,p.y);ctx.restore();}});
    fnParts.forEach(p=>{p.vx*=.97;p.vy=p.vy*.97+.07;p.x+=p.vx;p.y+=p.vy;p.a-=.011;if(p.a>0){ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=p.col;ctx.globalAlpha=p.a;ctx.shadowBlur=7;ctx.shadowColor=p.col;ctx.fill();ctx.globalAlpha=1;}});
    fnParts=fnParts.filter(p=>p.a>0);
    fnRAF=requestAnimationFrame(fnLoop);
  })();
}
document.getElementById('restart-btn').addEventListener('click',()=>{fnActive=false;cancelAnimationFrame(fnRAF);goTo(0);});

// Resize all canvases on window resize
addEventListener('resize',()=>{
  rsz();rszBurst();
  ['fw-c','fw-bloom','rk-c','rk-bloom','fn-c'].forEach(id=>{const c=document.getElementById(id);if(c){c.width=innerWidth;c.height=innerHeight;}});
});

console.log('%c🚀 Happy 11th Birthday, Joseph! 🎉','font-size:20px;color:#f5c518;font-weight:bold;background:#020510;padding:8px 16px;border-radius:4px');
</script>
<!-- GSAP + jQuery for envelope animation (loaded last so DOM is ready) -->
<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.2/gsap.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
</body>
</html>
